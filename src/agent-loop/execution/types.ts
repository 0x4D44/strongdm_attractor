/**
 * ExecutionEnvironment interface — re-export from parent types.
 * This file exists for discoverability; the actual interface is in ../types.ts.
 */

export type {
  ExecutionEnvironment,
  ExecResult,
  DirEntry,
  GrepOptions,
} from '../types.js';
